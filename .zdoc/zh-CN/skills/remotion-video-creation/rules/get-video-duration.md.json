{
  "sourceFile": "skills/remotion-video-creation/rules/get-video-duration.md",
  "sourceLanguage": "en-US",
  "entries": [
    {
      "en-US": "---\nname: get-video-duration\ndescription: Getting the duration of a video file in seconds with Mediabunny\nmetadata:\n  tags: duration, video, length, time, seconds\n---",
      "zh-Hans": "---\nname: get-video-duration\ndescription: 使用Mediabunny获取视频文件的时长（以秒为单位）\nmetadata:\n  tags: duration, video, length, time, seconds\n---"
    },
    {
      "en-US": "# Getting video duration with Mediabunny",
      "zh-Hans": "# 使用 Mediabunny 获取视频时长"
    },
    {
      "en-US": "Mediabunny can extract the duration of a video file. It works in browser, Node.js, and Bun environments.",
      "zh-Hans": "Mediabunny 可以提取视频文件的时长。它适用于浏览器、Node.js 和 Bun 环境。"
    },
    {
      "en-US": "## Getting video duration",
      "zh-Hans": "## 获取视频时长"
    },
    {
      "en-US": "```tsx\nimport { Input, ALL_FORMATS, UrlSource } from \"mediabunny\";\n\nexport const getVideoDuration = async (src: string) => {\n  const input = new Input({\n    formats: ALL_FORMATS,\n    source: new UrlSource(src, {\n      getRetryDelay: () => null,\n    }),\n  });\n\n  const durationInSeconds = await input.computeDuration();\n  return durationInSeconds;\n};\n```",
      "zh-Hans": "```tsx\nimport { Input, ALL_FORMATS, UrlSource } from \"mediabunny\";\n\nexport const getVideoDuration = async (src: string) => {\n  const input = new Input({\n    formats: ALL_FORMATS,\n    source: new UrlSource(src, {\n      getRetryDelay: () => null,\n    }),\n  });\n\n  const durationInSeconds = await input.computeDuration();\n  return durationInSeconds;\n};\n```"
    },
    {
      "en-US": "## Usage",
      "zh-Hans": "## 使用方法"
    },
    {
      "en-US": "```tsx\nconst duration = await getVideoDuration(\"https://remotion.media/video.mp4\");\nconsole.log(duration); // e.g. 10.5 (seconds)\n```",
      "zh-Hans": "```tsx\nconst duration = await getVideoDuration(\"https://remotion.media/video.mp4\");\nconsole.log(duration); // e.g. 10.5 (seconds)\n```"
    },
    {
      "en-US": "## Using with local files",
      "zh-Hans": "## 使用本地文件"
    },
    {
      "en-US": "For local files, use `FileSource` instead of `UrlSource`:",
      "zh-Hans": "对于本地文件，请使用 `FileSource` 而非 `UrlSource`："
    },
    {
      "en-US": "```tsx\nimport { Input, ALL_FORMATS, FileSource } from \"mediabunny\";\n\nconst input = new Input({\n  formats: ALL_FORMATS,\n  source: new FileSource(file), // File object from input or drag-drop\n});\n\nconst durationInSeconds = await input.computeDuration();\n```",
      "zh-Hans": "```tsx\nimport { Input, ALL_FORMATS, FileSource } from \"mediabunny\";\n\nconst input = new Input({\n  formats: ALL_FORMATS,\n  source: new FileSource(file), // File object from input or drag-drop\n});\n\nconst durationInSeconds = await input.computeDuration();\n```"
    },
    {
      "en-US": "## Using with staticFile in Remotion",
      "zh-Hans": "## 在 Remotion 中使用 staticFile"
    },
    {
      "en-US": "```tsx\nimport { staticFile } from \"remotion\";\n\nconst duration = await getVideoDuration(staticFile(\"video.mp4\"));\n```",
      "zh-Hans": "```tsx\nimport { staticFile } from \"remotion\";\n\nconst duration = await getVideoDuration(staticFile(\"video.mp4\"));\n```"
    }
  ]
}