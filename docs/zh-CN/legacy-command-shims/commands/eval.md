---
description: 用于 eval-harness 技能的旧版斜杠入口填充程序。建议直接使用该技能。
---

# Eval 命令（旧版兼容层）

仅当您仍在使用 `/eval` 时使用此命令。维护的工作流位于 `skills/eval-harness/SKILL.md`。

## 标准接口

* 优先直接使用 `eval-harness` 技能。
* 仅将此文件保留为兼容性入口点。

## 参数

`$ARGUMENTS`

## 委托

应用 `eval-harness` 技能。

* 支持与之前相同的用户意图：定义、检查、报告、列出和清理。
* 保持评估功能优先、回归驱动且基于证据。
* 使用该技能作为标准评估器，而不是维护独立的命令专用手册。
