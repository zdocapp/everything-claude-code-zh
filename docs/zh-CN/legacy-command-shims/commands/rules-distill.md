---
description: 用于 rules-distill 技能的旧版斜杠入口填充。建议直接使用该技能。
---

# 规则蒸馏（旧版兼容层）

仅当您仍调用 `/rules-distill` 时使用此文件。维护中的工作流位于 `skills/rules-distill/SKILL.md`。

## 规范接口

* 优先直接使用 `rules-distill` 技能。
* 仅将此文件保留为兼容性入口点。

## 参数

`$ARGUMENTS`

## 委托

应用 `rules-distill` 技能并遵循其清单、交叉审阅和裁决工作流，而非在此处重复该逻辑。
