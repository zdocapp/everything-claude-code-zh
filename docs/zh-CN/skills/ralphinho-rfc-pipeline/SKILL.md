---
name: ralphinho-rfc-pipeline
description: 基于RFC驱动的多智能体DAG执行模式，包含质量门、合并队列和工作单元编排。
origin: ECC
---

# Ralphinho RFC 流水线

灵感来源于 [humanplane](https://github.com/humanplane) 风格的 RFC 分解模式和多单元编排工作流。

当一个功能过于庞大，无法通过单次代理处理且必须拆分为可独立验证的工作单元时，请使用此技能。

## 流水线阶段

1. RFC 接收
2. DAG 分解
3. 单元分配
4. 单元实现
5. 单元验证
6. 合并队列与集成
7. 最终系统验证

## 单元规格模板

每个工作单元应包含：

* `id`
* `depends_on`
* `scope`
* `acceptance_tests`
* `risk_level`
* `rollback_plan`

## 复杂度层级

* 层级 1：独立的文件编辑，确定性测试
* 层级 2：多文件行为变更，中等集成风险
* 层级 3：架构/认证/性能/安全变更

## 每个单元的质量流水线

1. 研究
2. 实现计划
3. 实现
4. 测试
5. 审查
6. 合并就绪报告

## 合并队列规则

* 绝不合并存在未解决依赖项失败的单元。
* 始终将单元分支变基到最新的集成分支上。
* 每次队列合并后重新运行集成测试。

## 恢复

如果一个单元停滞：

* 从活动队列中移除
* 快照发现结果
* 重新生成范围缩小的单元
* 使用更新后的约束重试

## 输出

* RFC 执行日志
* 单元记分卡
* 依赖关系图快照
* 集成风险摘要
