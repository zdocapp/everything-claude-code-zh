---
description: 用于上下文预算技能的旧版斜杠入口垫片。建议直接使用该技能。
---

# 上下文预算优化器（旧版适配层）

仅当您仍调用 `/context-budget` 时使用此功能。维护中的工作流位于 `skills/context-budget/SKILL.md`。

## 规范使用方式

* 优先直接使用 `context-budget` 技能。
* 仅将此文件保留为兼容性入口点。

## 参数

$ARGUMENTS

## 委托处理

应用 `context-budget` 技能。

* 如果用户提供了 `--verbose`，则将其传递下去。
* 除非用户另有指定，否则假定上下文窗口为 200K。
* 返回该技能的清单、问题检测和优先级节省报告，无需在此处重新实现扫描。
