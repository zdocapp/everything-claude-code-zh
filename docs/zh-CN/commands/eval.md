---
description: 用于eval-harness技能的旧版斜杠入口垫片。建议直接使用该技能。
---

# Eval 命令（旧版适配器）

仅当您仍调用 `/eval` 时使用此命令。维护中的工作流位于 `skills/eval-harness/SKILL.md`。

## 规范使用方式

* 首选直接使用 `eval-harness` 技能。
* 仅将此文件保留为兼容性入口点。

## 参数

`$ARGUMENTS`

## 委托执行

应用 `eval-harness` 技能。

* 支持与之前相同的用户意图：定义、检查、报告、列出和清理。
* 保持评估以能力为先、有回归测试支持且基于证据。
* 使用该技能作为规范的评估器，而非维护独立的命令特定操作手册。
