# 循环状态命令

检查活动循环的状态、进度和故障信号。

## 用法

`/loop-status [--watch]`

## 报告内容

* 活动循环模式
* 当前阶段和最近一次成功的检查点
* 失败的检查项（如果有）
* 预估的时间/成本偏差
* 建议的干预措施（继续/暂停/停止）

## 监视模式

当存在 `--watch` 时，定期刷新状态并显示状态变化。

## 参数

$ARGUMENTS:

* `--watch` 可选
